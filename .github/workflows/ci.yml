name: CI Peadra

on:
  push:
    branches: [ "main" ]
  pull_request:

jobs:
  build:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: ["3.10", "3.11", "3.12"]

    steps:
    - uses: actions/checkout@v4

    - name: Set up Python ${{ matrix.python-version }}
      uses: actions/setup-python@v5
      with:
        python-version: ${{ matrix.python-version }}

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        if [ -f requirements.txt ]; then pip install -r requirements.txt; fi

    - name: Lint with Black
      continue-on-error: true
      run: |
        # Check formatting without modifying files
        black --check src tests

    - name: Lint with Pylint
      continue-on-error: true
      run: |
        # Run pylint on src directory
        # Disabling some common errors that might be noisy in a new project
        # C0111: Missing docstring (optional enforcement)
        # C0301: Line too long
        pylint src --disable=C0111,C0301

    - name: Test with pytest
      run: |
        # Run tests and generate coverage report
        # Add src to PYTHONPATH so tests can import modules
        export PYTHONPATH=$PYTHONPATH:$(pwd)
        pytest --cov=src tests/
