output: dist/

releases:
  - name: stable
    jobs:
      - name: build-linux-deb
        package:
          platform: linux
          target: deb
      - name: build-windows-msix
        package:
          platform: windows
          target: msix
